steps:
        . take input
        . sort the numbers array with a comperator as x+y>y+x;
                checking which is greator means 
                        let x=98 y=10 //strings
                        if(x+y>y+x)
                                then we want it 
                        otherwise
                                swap the elements; 
        . concatenate all the string into one largest number string 
        . print the result